TODOS
    Touchable should not be touchable opacity
        Should be TouchableWithoutFeedback
        Remove elevation/shadow onPress
    Add Linker component
    Add Sharer component
    Add WebViewer component
    Add Pusher component
    Add ToolTip component

NOTES
    should accept custom icons
    should accept custom styles
    text should accept text styles to change font if necessary
    USE STYLES INSTEAD OF BGCOLOR, SIZE etc
    all proptypes defined and commented
    dumb components where internal state and methods not needed
    all imports used
    methods in order
    Page should control component layout (always stretch to fill)